{
    "version": "2.0.0",
    "tasks": [
                {
            "label": "sjasmplus",
            "type": "shell",
            "command": "${fileDirname}/sjasmplus118.exe", 
            "args": [
                "--lst=${fileDirname}/trash/${fileBasenameNoExtension}.lst",
                "--sld=${fileDirname}/trash/${fileBasenameNoExtension}.sld",
                "--raw=${fileDirname}/trash/${fileBasenameNoExtension}.bin",
                "--fullpath",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
